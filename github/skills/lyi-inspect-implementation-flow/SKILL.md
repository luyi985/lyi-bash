---
name: lyi-inspect-implementation-flow
description: Inspect a repository to reconstruct the end-to-end implementation flow of a specified feature, event, API, function, message, command, job, or business behavior. Use when the user asks how logic is implemented, where a flow starts or ends, how data moves through the codebase, why an expected side effect does not occur, or which registrations, configurations, branches, adapters, producers, consumers, and dependencies participate in the behavior.
argument-hint: "[feature, event, API, function, message, command, job, or business behavior]"
---

# Inspect Implementation Flow

Read and follow:

`.github/ai/code-inspect-implementation-flow-instruction.md`

Treat that file as the authoritative workflow and output specification.

Use the current codebase as the primary evidence source.