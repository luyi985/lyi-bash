function packageInstaller() {
    [ -d $2 ] && echo -e "${green}Installing package $1 ($(pwd))${back}" && npm install $1
    [ -d $2 ] || (echo -e "${red}Fail installing package $1,not found($2)${back}" && return 1)
}

alias install=packageInstaller